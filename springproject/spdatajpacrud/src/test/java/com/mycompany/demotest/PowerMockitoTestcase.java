package com.mycompany.demotest;

/*
import org.junit.Test;
import org.junit.runner.RunWith;
import org.mockito.Mockito;
import org.powermock.core.classloader.annotations.PrepareForTest;
import org.powermock.modules.junit4.PowerMockRunner;

import static org.junit.Assert.assertEquals;
import static org.mockito.Mockito.times;
import static org.mockito.Mockito.verify;
import static org.powermock.api.mockito.PowerMockito.*;
*/

/*
 
 <dependency>
            <groupId>org.powermock</groupId>
            <artifactId>powermock-module-junit4</artifactId>
            <version>2.0.7</version>
            <scope>test</scope>
        </dependency>
        
        <dependency>
            <groupId>org.powermock</groupId>
            <artifactId>powermock-api-mockito2</artifactId>
            <version>2.0.7</version>
            <scope>test</scope>
        </dependency>
 
 
 */

/*
 @RunWith(PowerMockRunner.class)
@PrepareForTest(com.mycompany.demotest.StaticMethodTest.class)
 */

public class PowerMockitoTestcase {

	//@Test
    public void givenStaticMethods_whenUsingPowerMockito_thenCorrect() {
		/*
        mockStatic(StaticMethodTest.class);

        when(StaticMethodTest.firstMethod(Mockito.anyString())).thenReturn("Hello Baeldung!");
        when(StaticMethodTest.secondMethod()).thenReturn("Nothing special");
        doThrow(new RuntimeException()).when(StaticMethodTest.class);
        StaticMethodTest.thirdMethod();

        String firstWelcome = StaticMethodTest.firstMethod("Whoever");
        String secondWelcome = StaticMethodTest.firstMethod("Whatever");

        assertEquals("Hello Baeldung!", firstWelcome);
        assertEquals("Hello Baeldung!", secondWelcome);

        verifyStatic(StaticMethodTest.class, times(2));
        StaticMethodTest.firstMethod(Mockito.anyString());

        verifyStatic(StaticMethodTest.class, Mockito.never());
        StaticMethodTest.secondMethod();

        StaticMethodTest.thirdMethod();
        */
    }
	public static void main(String[] args) {
		// TODO Auto-generated method stub

		
	}

}
